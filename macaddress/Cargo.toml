[package]
name = "macaddress"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = { workspace = true, optional = true }
serde = { workspace = true, optional = true }

[features]
default = ["std", "serde"]
std = ["dep:thiserror", "serde/std"]
serde = ["dep:serde"]
